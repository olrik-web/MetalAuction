@model IEnumerable<EntityFrameworkModel.Køber>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.navn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tlf)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.navn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tlf)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KøberId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KøberId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KøberId })
        </td>
    </tr>
}

</table>
